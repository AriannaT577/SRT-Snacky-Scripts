#FOR loops
for number in range(0,102,2): #first number is where it starts, middle is the limit, and the third is the amount it goes up by
    print(str(number)+". Hello World!")
    #I predict that the PRINT code will run 10 times, as it is the range/limit of the number

#What similarities are there between a for loop and a while loop?
    #They are both loops that will run untill they fufill a certain condition. 
#What differences are there between a for loop and a while loop?
    #The FOR loop will run a certain number of times that is set by the programmer.
    #However, the WHILE loop will run untill its condition is turned FALSE. This means it may be dependant on the user input.
#How does the range function change when you include the second parameter?
    #The RANGE sets the mininum and the limit a number can be
    #It will start at 0, and count upwards a certain (The number inputted) amount of times.
    #However, adding a second number will decide the number's mininum, and where it would start at.
    #Adding a (1, 11) will make the program start at 1 (rather then the default of 0) and run 10 times (being within 11, not including 11).
